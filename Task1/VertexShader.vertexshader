attribute vec3 vertexPosition_modelspace; //объявление входных данных

void main(){
    // Присваиваем переменную отвечающей за отображение интпут полученный из буфера вида (позиция, прозрачность)
	gl_Position = vec4(vertexPosition_modelspace, 1.0); //RGB

}
